import { domain } from "hooks/customQueryClient";
import { useCenterData } from "../Center/useCenterData";
import useAnimal from "hooks/Animal/useAnimal";

interface WalkForm {
  date: string;
  time: number;
  center_uuid: string;
  animal_uuid: string;
}

function useWalkForm() {
  return <div>ㅇ</div>;
}

export default useWalkForm;
export {};
